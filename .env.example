# Database configuration
# For PostgreSQL: DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dbname
# For SQLite: DATABASE_URL=sqlite+aiosqlite:///./test.db
DATABASE_URL=sqlite+aiosqlite:///./test.db
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=30
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600

# Auth service configuration
AUTH_SERVICE_URL=http://localhost:8001

# Message queue configuration
# Use a secure URL with proper credentials in production
# Format: amqp://username:password@host:port/virtual_host
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# JWT configuration
# Generate a secure secret key for production
# Example command to generate: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment indicator (for validation)
ENVIRONMENT=development